# Medium Issues Validation — Part 1

## Error Handling

| # | Verdict | Justification |
|---|---------|---------------|
| M-1 | VALID | `src/app/api/books/upload/route.ts` L72-82: course created at L72, parse happens at L96-157. If parse fails, returns 400 at L153 but course row from L72 is never deleted — orphan remains. |
| M-2 | VALID | `src/app/api/courses/upload/route.ts` L38-42: course created at L38. Per-file parse errors (L81-85) are collected but route returns 200 at L88. If ALL files fail parsing, course row with 0 lessons persists. |
| M-3 | PARTIALLY VALID | Export lesson route (`export/lesson/[id]/route.ts` L248-253) catches `SyntaxError` from `JSON.parse` of DB data and returns 500 "Dữ liệu JSON trong DB bị lỗi" — this IS a server-side data issue, not bad client JSON. Malformed request JSON (invalid POST body) would fail at `req.json()` and fall to generic 500 at L255. Export course route (`export/course/[id]/route.ts` L159-165) has the same pattern. Claim partially valid: bad request JSON does get generic 500, but the SyntaxError catch is correctly identifying DB corruption not request corruption. |
| M-4 | VALID | `src/app/api/ai/models/route.ts` L23-25: `fetch(url, { headers })` has no timeout/AbortSignal. If upstream AI provider hangs, this request hangs indefinitely. |
| M-5 | PARTIALLY VALID | `src/app/api/udemy/courses/route.ts` L27-34: non-OK responses return `"Udemy trả về ${res.status}. Kiểm tra lại access_token."` — this does include the status code (L30) which differentiates errors, but the message always suggests bad token regardless of cause (e.g. rate limit 429, server error 500). The outer catch at L47-53 returns generic "Lỗi server" for network failures, which IS different from the token message. Claim overstated but partially true. |
| M-6 | VALID | `src/components/CollectionPanel.tsx` L68-80: on fetch error, `setData(null)` at L76. Render at L243 checks `filtered.length === 0` and shows "Chưa có ghi chú nào" / "Chưa có flashcard nào" — identical to empty state. No error state variable exists; error is indistinguishable from empty data. |
| M-7 | INVALID | `src/components/LessonList.tsx` L293-337: Re-split uses `AlertDialog` with `AlertDialogAction` at L313-329. The `onClick` handler is async (L315) and calls `await onReSplit()` at L318. The `AlertDialogAction` component from shadcn/radix closes the dialog on click, but the async operation runs independently — it does NOT swallow failures (errors propagate to `finally` block at L319). The dialog closing before completion is by design (radix AlertDialogAction), but errors are caught by the `finally` block and `reSplitLoading` state is reset. No evidence of failure swallowing. |
| M-8 | PARTIALLY VALID | `src/lib/ai/stream.ts` L98-104: errors during streaming are caught, `rejectFullText` is called, and `controller.error(err)` propagates the error to the stream consumer. However, the error reaches the client as an abrupt stream termination, not a structured JSON error (e.g. `{ error: "..." }`). Errors are not "silently swallowed" but they ARE not structured — the client sees a broken stream. |
| M-9 | VALID | `src/app/api/courses/[id]/profile/route.ts` L53-61: check-then-create pattern. `findUnique` at L53, then `create` at L65. Two concurrent POSTs can both pass the `findUnique` check (both see no existing profile), then one `create` succeeds and the other hits a unique constraint error caught by the generic catch at L77 returning 500 "Failed to create profile" instead of 409. |

## Race Conditions & State

| # | Verdict | Justification |
|---|---------|---------------|
| M-10 | VALID | `src/app/api/books/route.ts` L59-82: DELETE handler does `findUnique` at L59, checks conditions at L64-79, then `delete` at L82. Between the read and delete, another request could add lessons to the stub (via split/confirm), making the delete inconsistent with the earlier check. Classic TOCTOU. |
| M-11 | VALID | `src/app/api/ai/summary/route.ts` L44-46: cache guard checks `lesson.summary` at L44. Two concurrent requests for same lesson both see `summary` as null, both proceed to call AI and stream. Both then persist to DB at L75-85 via `prisma.lesson.update`. Duplicate AI computation and last-write-wins on DB. Same pattern in chat, explain, quiz routes. |
| M-12 | INVALID | `src/components/AIAssistantPanel.tsx` L342-346 & L672: `lastSavedChatCountRef` tracks saved count. After successful DB persistence at L660-672, `lastSavedChatCountRef.current = chatMessages.length + 2` (L672). The `onChatCountChange` effect at L344 computes `chatMessages.length - lastSavedChatCountRef.current`, which becomes 0 after save. The dirty flag IS cleared after successful persistence. |
| M-13 | PARTIALLY VALID | `src/components/UploadModal.tsx`: `handleClose` (L195) calls `handleReset` (L159) which aborts `abortControllerRef` — but this only covers BOOK upload flows. The transcript upload (`handleUploadTranscript` L315-363) uses plain `fetch` with no AbortController and no abort on close. Closing modal during transcript upload does NOT cancel the in-flight request. Claim valid for transcript uploads only. |
| M-14 | INVALID | `src/components/AnalyticsCourseDetail.tsx` L56-75: uses `AbortController` correctly. On `courseId` change, cleanup function aborts old request (L74). The `AbortError` is caught at L67 with `return` — it does NOT clear loading. The `finally` block at L69 sets `setLoading(false)`, but AbortError returns before reaching finally... Actually wait — the `return` at L67 skips `finally`? No — `finally` always runs. So aborted request DOES set `loading=false`. VALID concern: abort triggers `finally { setLoading(false) }` at L69-71, which clears loading state even though a new fetch is already in progress. |
| M-14 | VALID | (Corrected) L67 `return` inside catch skips the error state set, but `finally { setLoading(false) }` at L69-71 still executes after `return`. New fetch starts with `setLoading(true)` at L59, but the old abort's `finally` runs asynchronously and sets `setLoading(false)`, potentially clearing the new request's loading state. |
| M-15 | VALID | `src/components/CourseList.tsx` L115-132: `onKeyDown` Enter handler (L116-121) calls `onRename` and `setEditingId(null)`. `onBlur` handler (L126-131) also calls `onRename` if title changed and `setEditingId(null)`. When user presses Enter, the input loses focus (editingId set to null removes input), triggering `onBlur`. Since `setEditingId(null)` is async (React batching), the blur handler fires while `editingId` is still the course id, so `onRename` fires twice. |
| M-16 | PARTIALLY VALID | `src/hooks/useUrlState.ts` L33-47: `updateParams` reads `searchParams` via closure (dependency at L46). With `router.replace` (L44), rapid calls would each read the SAME `searchParams` snapshot until React re-renders with updated params. However, Next.js `useSearchParams` updates synchronously with `router.replace`, and the `useCallback` depends on `searchParams`. In practice, back-to-back calls within the same render cycle WILL use stale params. Partially valid — depends on timing. |
| M-17 | PARTIALLY VALID | `src/components/NotesEditor.tsx` L188-213: search debounce at 300ms. Clearing search (L318) sets `setSearchQuery("")` and `setSearchResults([])`. The `useEffect` at L188 checks `!searchQuery.trim()` and calls `setSearchResults([])` + returns. However, an in-flight fetch from the previous debounced call (L196-208) could resolve AFTER the clear and call `setSearchResults(data.results)`, overwriting the empty state. There's no abort mechanism for the in-flight search request. |
| M-18 | INVALID | `src/components/LearnerProfileModal.tsx` L99-129: lessons are loaded in `useEffect` with `[open, courseId]` dependencies. When `courseId` changes, the effect re-runs with `cancelled = false`, previous effect's cleanup sets `cancelled = true` preventing stale writes (L101, L107, L117, L121, L128). The `setLessons` at L110 only runs if `!cancelled`. Lesson options do NOT bleed across courses. |
