NEW-ISSUE-1 FIXED: Added splitBookIdRef = useRef<string | null>(null) mirroring splitBookId state. handleReset now deletes splitBookIdRef.current (the preview stub) in addition to pendingStubIdRef. The unmount useEffect cleanup also deletes splitBookIdRef.current. handleAnalyzeBook sets splitBookIdRef.current = createdBookId at the same time as setSplitBookId(). handleConfirmBook clears splitBookIdRef.current = null on success so a committed book is never orphan-deleted. handleCancelPreview sets splitBookIdRef.current = null before the DELETE fetch. All paths from close (X), click-outside, switchMode, and unmount now correctly clean up the stub regardless of which step the user is in.
